{
  "name": "grpc-web-debugger",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "static": "mkdir -p ./build && cp src/devtools.html src/panel.html ./build/",
    "compile": "tsc && npm run static",
    "watch": "npm run static && tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/chrome": "0.0.48",
    "tsc": "^1.20150623.0",
    "typescript": "^2.5.2"
  }
}
